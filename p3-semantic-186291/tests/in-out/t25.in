class Main {
    int sim = 0;
    public static void main(String[] args) {
        int n, r, temp;
        print("enter the number = ");
        n = 100100;
        temp = n;
        while(n > 0) {
            r = n % 10;
            this.sum = (this.sum * 10) + r;
            n = n / 10;
        }
        if(temp == this.sum)
            print("palindrome number ");
        else
            print("not palindrome");
    }
}